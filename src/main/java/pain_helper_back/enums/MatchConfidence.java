package pain_helper_back.enums;

/**
 * Уровень уверенности при сопоставлении patient from внешней системы с существующим patientом.
 * Используется в Patient Master Index for определения качества совпадения.
 */
public enum MatchConfidence {

    //Точное совпадение (100%): совпадают all ключевые поля (страховой полис + ФИО + дата рождения).
    EXACT,

    //Высокая уверенность (>90%): совпадают ФИО, дата рождения, пол.
    HIGH,

    //Средняя уверенность (70-90%): совпадают фонетически похожие имена + дата рождения.
    MEDIUM,

    //Нfromкая уверенность (<70%): частичные совпадения, требуется ручная проверка.
    LOW,

    //Совпадений not foundо - new patient.
    NO_MATCH
}
